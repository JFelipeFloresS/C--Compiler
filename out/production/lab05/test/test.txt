// minimum set of features
// variable definitions
int a; // example variable definition
int b; // example variable definition
int c; // example variable definition
double realNumber; // another variable definition

// function/procedure definitions
int add(int a, int b) {
int temp;
temp = a + b;
return temp;
}

// arrays
int[10] v; // v is an array of 10 ints
double[5][10] w; // w is a 2-dimensional array of doubles

//read/write
read v[i];
write w[i][a+b];

// if/else
if (a < b) {
    write a;
} else {
    write b;
}

if (a)
    write a;
else
    write b;

if (b)
    write b;

if (c) {
    write c;
}

// while
while (a < 10) {
    a = a + 1;
}

// cast
a = (int)realNumber;
realNumber = (double)a;

// function/procedure invocation expression vs statement
int result;
result = add(1, 2); // invocation as an expression
add(result, 1); // invocation as a statement

// expressions/assignments
a = 1 + 2 * 3 - 4 / 5;
a = (1 + 2) * (3 - 4) % 5;
a = -a;
a = !a;
if (a < b) {}
if (a <= b) {}
if (a > b) {}
if (a >= b) {}
if (a == b) {}
if (a != b) {}
if (a && b) {}
if (a || b) {}

// multiple definition of variables
int a, b, c;
double[10] realVector, anotherOne;

// multiple write/read
write a+b, v[45+c];
read realNumber, w[6][t];

// structs
struct {
int age;
struct { int day, month; int year; } dateOfBirth;
char[256] name, surname;
} [20] students; // 'students' is an array of 20 structs
students[0].dateOfBirth.day = 10;

struct {
int id;
char[30] name;
double price;
} product;
product.id = 1;

// mandatory main function
void main() {
    int result;
    result = add(1, 2); // invocation as an expression
    add(result, 1); // invocation as a statement
}